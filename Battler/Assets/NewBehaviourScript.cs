using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
       Scene 0: Splash screen - Start / Exit
       Scene 1: 
       Choose Character
       AI Choose Character - Non visable
       Scene 2:
       Enter Fight
       Player go
       Choose Move
           Move action
       Resolve action
       AI Choose Move
           Move Action
       Resolve Action
       - Repeat until health 0
           Resolve Battle
           Win / Lose
               Retry?
    */
    /*
     Character - 
        Health
        Moves
        Base Stats
     */
    /*
     AI - as Character with decision generator
     */
    /*
     UI 
        - Start screen
        - Character select
        - Fight UI
        - Resolution display
     */
}
